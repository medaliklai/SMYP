<%- include('../partiels/header.ejs') %>
    <div class="main-content">
        <%- include('../menu.ejs') %> 
        <div class="content">
            <h2>Liste des temps</h2>
            <button class="btn btn-info"><a href="/ajouter_temp">Ajouter</a></button>
            <% if (temps.length>0){ %>
            <table  class="table">
            <tr>
                <td>Valeur</td>
                <td>Année</td>

            </tr>
            <% for( temp of temps ) { %>
                <tr>

                    <td><%= temp.valeur %> </td>
                    <td> <%= temp.annee %></td>

                    <td><button class="btn btn-danger" data-doc="<%= temp._id %> ">Supprimer</button></td>
                    <td><button class="btn btn-primary"><a href="/modifier_temp/<%= temp._id %> ">Modifier</a></button></td>

                    </tr>
            <% } %>


        </table>
            <% } else{ %>
<div class="vide">Aucune temp</div>
                <% }  %>

    </div>
    </div>
    <script>
        const btnSupprimer = document.querySelector('.btn-danger');
        btnSupprimer.addEventListener('click',(e)=>{
            fetch(`/temp/${btnSupprimer.dataset.doc}`,{
                method:'DELETE'
            })
            .then((result)=>{
                if(result.ok){
                    location.assign('/temp');
                }
            })
        })
    </script>

   